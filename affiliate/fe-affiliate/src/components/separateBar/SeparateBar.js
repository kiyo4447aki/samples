import styled from "styled-components"

const SeparateBar = () => {
	return <SeparateBarWrapper>｜</SeparateBarWrapper>
}

const SeparateBarWrapper = styled.p`
	color: #333;
	font-family: Meiryo;
	font-size: 10.828px;
	font-style: normal;
	font-weight: 400;
	line-height: 15.4px;
	text-decoration: none;
`

export default SeparateBar
